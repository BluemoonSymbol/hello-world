#include <stdio.h>
 
#include <windows.h>
 
int main()
 
{
 
   char a[100], b[100];
 
   char *temp;
 
   int counter = 0;
 
   memset( a, sizeof(a), 0 ); //????
  
   memset( a, sizeof(b), 0 ); //????
       
   printf( "Please input source string: " );
 
   gets(a); //??????????. 
  
   printf( "Please input find string: " );
 
   gets(b); //???????????. 
  
   temp = a; //???????????.
  
   while( temp )
  
   {
 
      temp = strstr( temp, b ); //????????
//????????,????????,?????NULL. 
      if( temp != NULL ) //?????
//,????????????,??????,??????. 
      {
 
         temp += strlen(b);
  
         counter++;
 
      }
 
   }
 
   printf( "find %d times!\n", counter ); //???????.
  
   system("pause");
 
   return 0;
 
}
